import barangEndpoints from './barang'
import transaksiEndpoints from './transaksi'
import detailTransaksiEndpoints from './detail_transaksi'

const endpoints = {
    barangs: barangEndpoints,
    transaksis: transaksiEndpoints,
    detail_transaksis: detailTransaksiEndpoints
}

export default endpoints